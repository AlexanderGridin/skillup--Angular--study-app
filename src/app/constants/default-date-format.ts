import { DateFormat } from '../interfaces/date-format';

export const DEFAULT_DATE_FORMAT: DateFormat = { date: 'dd/MM/yyyy' };
// export const DEFAULT_DATE_FORMAT: DateFormat = { date: 'MM/dd/yyyy' };
